{form changePassword}
    <div class="form-group">
        {label password}{/label}
        {input password, class => 'form-control'}
    </div>

    <div class="form-group">
        {label checkPassword}{/label}
        {input checkPassword, class => 'form-control'}
    </div>

    {input process, class => 'btn btn-success'}
{/form}